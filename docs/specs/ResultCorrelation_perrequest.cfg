\* Per-request result list (api.RedisRequest.ResultQueueName) -> property HOLDS.
CONSTANTS
    Callers = {c1, c2}
    PerRequestQueue = TRUE
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT EachCallerGetsOwnResult
