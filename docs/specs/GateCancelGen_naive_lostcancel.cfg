\* id-only design -> NoLostCancel VIOLATED (the ABA: gen 1's stale cleanup wipes
\* active, so Cancel no-ops and gen 2 dispatches despite being cancelled).
CONSTANTS
    MaxGen = 2
    TokenGuarded = FALSE
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT NoLostCancel
