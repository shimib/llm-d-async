\* id-only design -> NoSpuriousCancel VIOLATED (gen 1 matches a cancel marker meant
\* for gen 2 and is dropped, because IsCancelled only checks that a marker exists).
CONSTANTS
    MaxGen = 2
    TokenGuarded = FALSE
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT NoSpuriousCancel
