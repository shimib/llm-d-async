\* Current code: retry never releases -> AccountingExact VIOLATED (Store overwrite
\* double-counts inFlight while the id is reserved only once).
CONSTANTS
    Requests = {r1, r2}
    Limit = 2
    MaxAttempts = 1
    ReleaseOnRetry = FALSE
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT AccountingExact
