\* Current code, liveness: a retried request's un-released reservation occupies the
\* only slot, so it can never be re-dispatched -> EventuallyServed VIOLATED (wedge).
CONSTANTS
    Requests = {r1, r2}
    Limit = 1
    MaxAttempts = 1
    ReleaseOnRetry = FALSE
SPECIFICATION Spec
PROPERTY EventuallyServed
